# Configuração local do GeneXus Next — contém credenciais, NUNCA versionar
settings-overrides.json

# Junctions geradas por setup-skills.ps1 (fonte única = .claude/skills) — não versionar
/.agents/
/.codex/

# Ruído de SO / editor
Thumbs.db
.DS_Store
*.log
